fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
python-dotenv>=1.0
httpx>=0.28
anthropic>=0.42

# Tests
pytest>=8.0

# Optional: real Cognee Cloud / self-hosted memory backend.
# Verified against cognee 1.x (remember / recall / improve / forget).
# Cognee currently requires Python <= 3.12 (see Dockerfile: python:3.12-slim).
# If it is not installed, Lethe runs automatically on the local memory backend.
# cognee>=1.2
